{
  "compilerOptions": {
    "strict": true,
    "noImplicitReturns": true,
    "noFallthroughCasesInSwitch": true,
    "noImplicitOverride": true,

    "baseUrl": "src",
    "paths": {
      "common/*": ["common/*"]
    }
  },
  "files": [], // disables base config's output
  // setting `"noEmit": true` in the compiler options yells at me.
  "references": [
    {
      "path": "./tsconfig.dom.json"
    },
    {
      "path": "./tsconfig.node.json"
    }
  ]
}